# Solutions — Mathematics (Class 7), Chapter 9: Geometric Twins — Paper 5

**Paper 5 — (progressively harder)**

Marking: **+4** correct, **−1** wrong, **0** unattempted. Maximum marks **240**.

<!--
Coverage matrix (HARDEST tier, Paper 5):
- Congruence criteria stress-tested at limits (SSS/SAS/ASA/AAS/RHS vs SSA/AAA), multi-condition deductions: ~12
- CPCT, correspondence from letter order, self-congruence / cyclic correspondence: ~9
- Isosceles & equilateral angle chains, nested isosceles, exterior-angle layering: ~13
- Multi-step numeric: Pythagoras on isosceles/right triangles, altitude/median/area, angle algebra & ratios: ~12
- Symmetry: lines vs rotational order, reflections, combined transforms, letters/polygons, interplay theorems: ~14
-->

## Answer Key

| Q | Ans | Q | Ans | Q | Ans | Q | Ans | Q | Ans | Q | Ans |
|---|-----|---|-----|---|-----|---|-----|---|-----|---|-----|
| 1 | B | 11 | D | 21 | C | 31 | B | 41 | C | 51 | B |
| 2 | A | 12 | D | 22 | A | 32 | D | 42 | B | 52 | D |
| 3 | A | 13 | C | 23 | A | 33 | A | 43 | C | 53 | C |
| 4 | B | 14 | B | 24 | D | 34 | D | 44 | A | 54 | B |
| 5 | C | 15 | A | 25 | B | 35 | B | 45 | D | 55 | B |
| 6 | C | 16 | C | 26 | D | 36 | A | 46 | A | 56 | D |
| 7 | A | 17 | D | 27 | C | 37 | C | 47 | A | 57 | D |
| 8 | C | 18 | C | 28 | B | 38 | A | 48 | D | 58 | B |
| 9 | B | 19 | D | 29 | C | 39 | B | 49 | C | 59 | B |
| 10 | A | 20 | D | 30 | D | 40 | A | 50 | A | 60 | C |

Answer-key distribution: A = 15, B = 15, C = 15, D = 15.

---

## Worked Solutions

**1. (B)** Drop the altitude from the apex A to the base; because AB = AC, its foot M is the midpoint of BC, so BM = 30/2 = 15 cm and angle AMB = 90°. In right triangle ABM, AM = √(AB² − BM²) = √(17² − 15²) = √(289 − 225) = √64 = 8 cm. Since P is on BC with BP = 9 cm and BM = 15 cm, P lies between B and M with PM = BM − BP = 15 − 9 = 6 cm, and AM is perpendicular to BC so triangle AMP is right-angled at M. Hence AP = √(AM² + PM²) = √(8² + 6²) = √(64 + 36) = √100 = 10 cm. The value 8 cm is only the altitude AM itself and ignores the horizontal offset PM; √145 cm comes from using BP = 9 directly as the horizontal leg (√(8² + 9²)) instead of the correct PM = 6; 12 cm comes from mis-taking the altitude foot as lying 9 cm from B and then mis-adding, and in any case the shortest distance from A to a point on BC is the altitude 8 cm, so AP just past the foot is only slightly more than 8.

**2. (A)** Angle B = 90° sits opposite side AC, so AC is the hypotenuse (12 cm) and AB is a leg (9 cm). Equal right angle, equal hypotenuse and one equal leg is exactly RHS, which is a valid criterion, so they are congruent. The SSA-objection fails because RHS is precisely the safe special case of side-side-right-angle. SAS is wrong because the right angle at B is NOT included between AB and AC (AC does not start at B). 'Only similar' ignores that all three pairs of parts are equal in length.

**3. (A)** Base angles: angle ABC = angle ACB = (180−36)/2 = 72°. BP bisects angle B, so angle ABP = angle PBC = 36° each. (Indeed BP = BC is consistent: triangle BPC has angle PBC = 36° and angle BCP = 72°, so angle BPC = 72°, making BP = BC.) angle ABP = 36°. The trap answer 72° is the whole base angle (not the bisected half); 18° halves the apex instead of the base angle; 24° splits 72° into three.

**4. (B)** Composing two reflections in lines that meet at angle θ gives a rotation through 2θ; perpendicular lines (θ = 90°) compose to a 180° rotation, forcing at least order 2. It is not forced to order 4 — that would need a third pair of mirror lines. A non-square rectangle is exactly such a figure (2 mirror lines, order 2), so it need NOT be a square, and any regular polygon with 5+ sides has 5+ mirror lines, contradicting 'exactly two'. Order 1 is wrong because the 180° rotation is genuine.

**5. (C)** angle B = angle C makes AB = AC = 13 cm (isosceles), and the altitude from the apex to the base lands at the midpoint, with right angle at M. In right triangle ABM, AM² = AB² − BM² = 169 − 25 = 144, so AM = 12 cm. The 8 comes from 13 − 5; 18 from 13 + 5; √194 from wrongly adding 169 + 25.

**6. (C)** Chaining the correspondences A↔D↔P, B↔E↔Q, C↔F↔R. So angle A = angle D = angle P = 50°, and angle Q = angle E = angle B = 70°. The angle sum gives angle C = 180 − 50 − 70 = 60°. Picking 50° or 70° just echoes a given angle for the wrong vertex; 'cannot be determined' ignores that the correspondences pin down both other angles.

**7. (A)** Base angles are (180−100)/2 = 40° each. The bisectors halve them to 20° each, so in triangle BIC the angles at B and C are 20° each and angle BIC = 180 − 20 − 20 = 140°. (Equivalently angle BIC = 90 + A/2 = 90 + 50 = 140°.) 120° uses 90 + 30; 100° just copies the apex; 130° comes from 90 + 40.

**8. (C)** Because the right angle is at A, the side opposite it, BC, is the hypotenuse. We therefore have equal hypotenuse (8 cm), one equal leg (6 cm) and the right angle — that is RHS, which does guarantee congruence. SAS is wrong because the right angle at A is included between AB and AC, not between AB and BC. It is not the unsafe SSA pattern precisely because the equal angle is the right angle (RHS is the legitimate special case). 'Only similar' is wrong since the third side is then forced equal too.

**9. (B)** A regular n-gon has smallest rotation 360°/n, so 360/n = 24 gives n = 15. A regular polygon has exactly n lines of symmetry, hence 15. Answer 24 confuses the angle with a count; 12 halves n; 30 doubles it.

**10. (A)** The self-correspondence A↔C, B↔B, C↔A says, by CPCT, side BA = side BC and angle A = angle C. That makes the triangle isosceles with the apex at B; nothing forces all three sides equal, so it need not be equilateral. The apex is at B (the fixed letter), not A, so 'AB = AC' is the wrong pairing. No right angle is implied.

**11. (D)** M and N are midpoints of two sides, so MN is the midsegment parallel to the third side AC and equal to half of it: MN = AC/2 = 10/2 = 5 cm. The distractor 6 = BC/2 uses the wrong side; 8 is the altitude AM (√(100−36)); 4 is an unrelated guess. (The numbers 10, 12 are there to tempt a Pythagorean detour that is not needed.)

**12. (D)** Triangles ABE and ACD share angle A; AB = AC and AE = AD are given, so SAS proves them congruent, giving BE = CD and angle ABE = angle ACD. Since angle ABC = angle ACB (base angles) we get angle OBC = angle OCB, so triangle OBC is isosceles with OB = OC. SSS is wrong (no third pair of sides given directly); ASA is wrong because the common angle A is included between the two equal sides, making it SAS not ASA; AAA never yields congruence and the relation OB = OC genuinely holds.

**13. (C)** The angles are 40°, 60°, 80° — all different, so the triangle is scalene and has 0 lines of symmetry; reflection does not change that. A reflection is a rigid motion: it preserves every length and angle, so the image is congruent to the original (congruence allows a flip). 'Only similar' wrongly treats a flip as a loss of congruence; '3 lines' is the rule for equilateral, not scalene; the orientation reversal does not break congruence.

**14. (B)** With angle A = 90° and AB = AC, the triangle is an isosceles right triangle; the altitude from the right angle to the hypotenuse lands at its midpoint and equals half the hypotenuse: AD = BC/2 = 8/2 = 4 cm. (In a right triangle the median to the hypotenuse equals half it, and here the altitude coincides with that median.) 4√2 wrongly applies a leg formula; 8 forgets to halve; 2 quarters it.

**15. (A)** Corresponding angles are equal, so 2x + 15 = 3x − 5, giving x = 20 and angle A = angle D = 55°. In triangle ABC the angle sum gives angle C = 180 − angle A − angle B = 180 − 55 − 50 = 75°. Since angle F corresponds to angle C, angle F = 75°. The trap 55° is angle A/angle C confusion (it is angle A's value, not angle C's); 50° just echoes angle B; 65° comes from a wrong angle sum.

**16. (C)** Equal angles give similarity with some scale factor k, so each side of the second is k times the matching side of the first; the perimeter scales by the same k. Equal perimeters force k = 1, hence equal corresponding sides and congruence. The blanket 'AAA never gives congruence' ignores the extra equal-perimeter condition. The 'differently shaped' objection contradicts the given equal angles (same shape). It holds for all triangles, not just equilateral ones.

**17. (D)** Since AB = AC, angle B = angle C. Let angle A = a; then angle B = angle C = 2a. Sum: a + 2a + 2a = 180, so 5a = 180, a = 36°, and angle C = 2a = 72°. The trap 36° is angle A (the apex), not angle C; 45° and 54° come from wrong split assumptions.

**18. (C)** N maps onto itself under a 180° turn (order-2 rotational symmetry) yet has neither a vertical nor horizontal mirror line — its diagonal stroke breaks both. H and X each have BOTH a vertical and horizontal line of symmetry (so they do have mirror lines), and T has only a vertical mirror line with no order-2 rotation. So N is the unique fit.

**19. (D)** BD = DC means D is the midpoint of BC, and AD = BD = DC means D is equidistant from A, B, C, so D is the circumcentre lying on BC — i.e. BC is a diameter and A is on the circle, making angle BAC the angle in a semicircle = 90°. More elementarily: triangles ABD and ACD are isosceles, so angle ABD = angle BAD and angle ACD = angle CAD; their sum 2(angle BAD + angle CAD) = 180° gives angle BAC = angle BAD + angle CAD = 90°. 60° assumes equilateral; 45° halves wrongly; it is fully determined.

**20. (D)** Two angles and the side BETWEEN them are equal: BC lies between angle B and angle C, the textbook ASA pattern, which proves congruence. SAS requires two SIDES and the included angle, but only one side is given here, so calling it SAS is wrong. It is not SSA (no two sides given) and not mere AAA (the equal side BC fixes the size).

**21. (C)** The exterior angle at a base vertex is 110°, so the interior base angle there is 180 − 110 = 70°. In an isosceles triangle the two base angles are equal, so both are 70°, and the apex angle = 180 − 70 − 70 = 40°. The trap 70° gives the base angle, not the apex; 110° reuses the exterior angle; 20° doubles-counts incorrectly.

**22. (A)** Triangle ABC has base 6, half-base 3, height √(25−9) = 4, area = 12 cm². An isosceles triangle with equal sides 5 and base b has height √(25 − b²/4) and area = (b/2)·√(25 − b²/4). Setting this to 12 gives b²(100 − b²) = 2304, whose solutions are b = 6 AND b = 8 (the 'tall' and 'short' twins, since for b = 8 the height is 3 and area is again 12). So a non-congruent DEF with EF = 8 exists. Equal two sides plus equal area therefore does NOT force the third side; SAS needs an equal included angle (not given); the mirror-image claim is unjustified.

**23. (A)** Reflection is a rigid motion: it preserves every length (hence the perimeter, 30 cm) and the size of the enclosed region (hence the area, 35 cm²); only the left/right orientation is reversed. Areas are NOT changed by a flip, the figure is not duplicated, and nothing is halved.

**24. (D)** Let angle A = a; the equal base angles are angle ABC = angle ACB = (180−a)/2. BD bisects angle B, so angle DBC = (180−a)/4. In triangle BDC the angle sum gives angle BDC = 180 − angle DBC − angle ACB = 180 − (180−a)/4 − (180−a)/2 = 180 − 3(180−a)/4. Setting this to 105°: 3(180−a)/4 = 75, so 180 − a = 100, a = 80°. The trap 40° is the base-angle value when a = 100; 50° and 70° come from dropping the bisector factor.

**25. (B)** Order 6 means the mapping angles are multiples of 360/6 = 60°: 60°, 120°, 180°, 240°, 300° and 360°. Counting only angles STRICTLY less than 360° excludes the 360° (full turn), leaving five: 60, 120, 180, 240, 300. The answer 6 wrongly includes the full turn; 60 is the smallest angle, not a count; 12 doubles.

**26. (D)** All three pairs of sides match (7, 9, 7 against 7, 9, 7), so SSS proves congruence. Within each triangle AB = BC = 7 cm, so each is isosceles and the angles opposite the equal sides — angle C (opposite AB) and angle A (opposite BC) — are equal. SAS is wrong because no angle was provided; 'only similar' ignores that equal sides also fix size; the third sides do match (7 = 7).

**27. (C)** In ABC the hypotenuse (opposite the right angle at B) is AC = √(9² + 12²) = √(81 + 144) = √225 = 15 cm, the longest side. Congruent DEF has the same longest side, 15 cm. 12 is just the longer leg; 21 = 9 + 12; √63 wrongly subtracts.

**28. (B)** Each symmetry of an equilateral triangle (its 3 mirror lines and its order-3 rotations) maps the triangle onto itself. The reflection in the perpendicular bisector of BC is one of those mirror lines, and a 120° rotation is one of its rotational symmetries; composing two self-maps is still a self-map, so the final figure coincides with the original region. It is not misaligned, not an un-matchable mirror image, and transformations never change size.

**29. (C)** Base angles: angle B = angle C = (180−40)/2 = 70°. CE bisects angle C, so angle ACE = 35°. In triangle AEC, angle AEC = 180 − angle A − angle ACE = 180 − 40 − 35 = 105°. The trap 70° is the base angle itself; 110° comes from 180 − 70; 100° from 180 − 40 − 40.

**30. (D)** A rhombus's two lines of symmetry are its diagonals (vertex-to-vertex), and it has order-2 rotational symmetry — matching exactly. A non-square rectangle's two mirror lines pass through the MIDPOINTS of opposite edges, not the vertices, so it fails the 'through vertices' condition. A square has FOUR mirror lines (and order 4). An isosceles trapezium has only ONE line of symmetry and order 1.

**31. (B)** In triangle DEF, EF is opposite vertex D and DF is opposite vertex E. EF = DF therefore means the opposite angles are equal: angle D = angle E. Since angle D corresponds to angle A = 70°, we get angle D = 70°, so angle E = 70° as well. angle B corresponds to angle E, hence angle B = 70°. The trap 55° comes from treating DE (not EF = DF) as the equal pair and splitting the remainder; 40° is the apex angle F = 180 − 70 − 70; 'cannot be determined' ignores the isosceles deduction.

**32. (D)** Let each base angle be b; apex = 4b. Sum: b + b + 4b = 180, so 6b = 180, b = 30°, and apex = 4b = 120°. The trap 90° comes from 4b = 4·... mis-solved; 100° and 144° from wrong totals.

**33. (A)** For SAS the equal angle must lie BETWEEN the two equal sides. Here the two equal sides are AB and BC, whose included angle is angle B, but the given equal angle is angle A — opposite side BC. That is the side-side-angle (SSA) pattern, which does not guarantee congruence (two non-congruent triangles can satisfy it). So it is not SAS, not ASA (only one angle given), and angle A is not stated to be a right angle so RHS does not apply.

**34. (D)** First the area: drop the altitude from A to BC; half-base = 15, height = √(25² − 15²) = √(625−225) = √400 = 20, so area = (1/2)(30)(20) = 300 cm². Now BP is the altitude onto AC, so area = (1/2)(AC)(BP) = (1/2)(25)(BP) = 300, giving BP = 600/25 = 24 cm. The 20 is the altitude from A (different altitude); 15 is half the base; 7 is unrelated.

**35. (B)** Order-5 rotational symmetry plus 5 mirror lines is the symmetry of a regular pentagon; requiring a single closed polygon with all sides equal pins it to the regular pentagon. A pentagram is not a simple (non-self-intersecting) closed polygon. A regular decagon would have order 10 and 10 mirror lines, too many. 'Any equilateral pentagon' is wrong because equal sides alone do not force equal angles (an equilateral pentagon can be irregular and lack the 5-fold symmetry).

**36. (A)** In isosceles triangle ABC with AB = AC, the bisector of the apex angle is also the median to the base, so it bisects BC: DB = DC. Hence DC = 7 cm. 14 doubles; 3.5 halves; it is fully determined by the isosceles symmetry (triangles ABD and ACD are congruent by SAS).

**37. (C)** Third angle in each is 180 − 40 − 60 = 80°. The equal side (10 cm) lies opposite the 80° angle, which is angle C (= F). With two equal angles and a corresponding equal side that is not between them, this is AAS — a valid congruence criterion (once two angles match, the third does too, so it reduces to ASA). SAS needs two equal SIDES (only one is given). It is not the unsafe SSA, and not mere AAA because a real side length is fixed.

**38. (A)** angle A = angle B = angle C forces an equilateral triangle, which has exactly 3 lines of symmetry (each through a vertex and the opposite side's midpoint) and rotational symmetry of order 3 (120° and 240° turns). '1 and 1' describes a scalene/generic triangle; order 6 and 6 mirror lines overcount (that is a regular hexagon's pattern, not a triangle's).

**39. (B)** Parts sum to 2 + 3 + 7 = 12, so one part = 180/12 = 15°. The largest angle is 7 parts = 7 × 15 = 105° (angle C), and the congruent DEF has the same largest angle. 90° wrongly assumes a right angle; 70° uses 180×7/18 type errors; 84° comes from a wrong part value.

**40. (A)** In isosceles triangle ABC with AB = AC, the altitude AD from the apex is also the perpendicular bisector of the base BC, so BD = DC and every point on line AD (including E) is equidistant from B and C, giving EB = EC for any E on AD. Hence the figure is symmetric about AD. E need not be the midpoint of AD; BD = DC always holds here; no right angle at A is implied.

**41. (C)** Smallest rotation 45° gives the smaller polygon n = 360/45 = 8 sides (a regular octagon). Twice as many sides means 16 (a regular hexadecagon), whose smallest rotation is 360/16 = 22.5° and which has 16 mirror lines. 90°/4 halves the side count; '22.5° and 8' keeps the small count; '45° and 16' fails to update the angle.

**42. (B)** The self-correspondence A↔B, C↔C forces CA = CB and angle A = angle B (isosceles with apex C). With angle C = 50°, the two equal base angles are (180−50)/2 = 65° each, giving 65°, 65°, 50°. '50, 50, 80' wrongly makes C a base angle; equilateral 60s ignore angle C = 50°; '90, 40, 50' invents a right angle.

**43. (C)** Use angle BIC = 90° + (angle A)/2 = 90 + 40 = 130° (a standard result for the incentre). Check directly: base angles = (180−80)/2 = 50° each, halved to 25°, so angle BIC = 180 − 25 − 25 = 130°. The trap 100° copies double-the-base; 140° uses A/2 = 50; 120° uses A/2 = 30.

**44. (A)** AB = AC = 8 cm with included angle A = 60° is SAS, so the triangles are congruent. Moreover a triangle with two equal sides and a 60° included angle has its base angles each (180−60)/2 = 60°, so all angles are 60° and the third side is also 8 cm — it is equilateral. SSS is wrong because the third side was not GIVEN (it is deduced); 'only similar' ignores the equal SAS data; 'never equilateral' is false here.

**45. (D)** Reflecting in two parallel lines a distance d apart is equivalent to a translation of 2d in the direction perpendicular to the lines. Here d = 3 cm, so the net effect is a translation of 6 cm. Two reflections give an even (orientation-preserving) isometry, so the result is not a single reflection; parallel mirrors give a translation, not a rotation; the distance is 2d = 6, not 3.

**46. (A)** AC = √(6² + 8²) = 10 cm. The median to the hypotenuse of a right triangle equals half the hypotenuse, so BM = AC/2 = 10/2 = 5 cm. (M is equidistant from A, B, C — the circumcentre.) 10 forgets to halve; 4 is unrelated; √50 wrongly Pythagoras-combines.

**47. (A)** By the correspondence A↔D, B↔E, the side DE corresponds to side AB, so AB = DE = 9 cm — this follows purely from the congruence and letter order, regardless of the angle fact. (The angle A = angle B is extra and tells us the triangle is isosceles but is not needed here.) BC corresponds to EF and AC to DF, neither of which equals DE; 'nothing' ignores the correspondence.

**48. (D)** Let each base angle be b; apex = 2b (the sum of the two base angles). Total: 2b (apex) + b + b = 180, so 4b = 180, b = 45°, apex = 2b = 90°. The trap 60° assumes equilateral; 120° and 45° come from wrong setups. (This is just the right-angled isosceles triangle.)

**49. (C)** Knowing AB, AC and the median m_a to BC actually pins the triangle: reflect A over the midpoint M of BC to a point A'; then ABA'C is a parallelogram, so AA' = 2·m_a and the triangle ABA' has AB, AA' = 2m_a and BA' = AC fixed — SSS fixes it, hence the original triangle is determined up to congruence. So equal AB, AC and equal median force congruence. 'Medians irrelevant' is false; 'only similar' ignores the fixed lengths; it is NOT plain SAS because angle A is not given (the median substitutes for it).

**50. (A)** If a figure has rotational symmetry of order 3 (rotations by 120° and 240°) AND one mirror line, then rotating that mirror line by the 120° symmetries produces two more mirror lines, so there must be at least 3 lines of symmetry in total. Having just 1 is impossible alongside order-3 rotation; 6 would require order-6 rotation; 2 cannot occur with odd-order rotation (mirror lines come in a set matching the rotation order). So the minimum is 3.

**51. (B)** Base angles: angle B = angle C = (180−20)/2 = 80°. BD bisects angle B, so angle DBC = 40°. In triangle BDC the angle sum gives angle BDC = 180 − angle DBC − angle BCD = 180 − 40 − 80 = 60°. The trap 80° just reuses a base angle; 100° = 180 − 80; 120° forgets to bisect angle B (using 40 with a wrong remainder).

**52. (D)** A reflection is a rigid (distance-preserving) motion, so every length and angle is preserved — equal sides, equal angles, equal perimeter and equal area. But unlike a rotation or translation, a single reflection REVERSES orientation (left becomes right). So handedness is reversed, not preserved; angles are not changed; both area and perimeter are preserved.

**53. (C)** Because AB = AC, the altitude from the apex A bisects the base, so BM = 18/2 = 9 cm and angle AMB = 90°. In right triangle ABM, AM² = AB² − BM² = 15² − 9² = 225 − 81 = 144, so AM = 12 cm. (The point N is a distractor not needed for AM.) 9 is half the base BM, not AM; √306 wrongly adds 225 + 81; 6 halves AM.

**54. (B)** The angles of DEF sum to 180: (x + 40) + 2x + (x − 10) = 180, so 4x + 30 = 180, 4x = 150, x = 37.5. Then angle E = 2x = 75°, and angle B corresponds to angle E, so angle B = 75°. The trap 77.5 = x + 40; 70 mis-solves; 37.5 reports x itself.

**55. (B)** A 90° rotation that maps the figure onto itself forces all multiples — 90°, 180°, 270°, 360° — so the rotational order is at least 4. The figure can still have NO mirror lines (e.g. a pinwheel/swastika-like motif), so 0 mirror lines is consistent. 'Order 4 and 4 mirror lines' wrongly assumes a square's full symmetry; order 2 is too small (a 90° turn implies order ≥ 4); 'order 90' confuses the angle with the order.

**56. (D)** In the triangle ABD (using the half-angles at A and B), angle ADB = 180 − (A/2 + B/2) = 110, so A/2 + B/2 = 70, giving A + B = 140. Then angle C = 180 − (A + B) = 180 − 140 = 40°. (Equivalently angle ADB = 90 + C/2, so 110 = 90 + C/2, C = 40°.) The trap 55 = solving for C/... ; 70 = A/2 + B/2; 20 halves wrongly.

**57. (D)** Sides 5, 6, 5 match 5, 6, 5, so SSS gives congruence. In each triangle AB = CA = 5, so it is isosceles with apex A, making the base angles angle B = angle C equal. The apex angle is angle BAC = angle A = angle D (corresponding), NOT angle DEF (which corresponds to angle B). So angle BAC = angle EDF, not angle DEF — the student's last equality is wrong. 'Only similar' and 'not isosceles' are both false; it is SSS not SAS, and not equilateral (5 ≠ 6).

**58. (B)** The perimeter detail is a distractor — it does not affect symmetry counts. A square has rotational symmetry of order 4; an equilateral triangle has order 3. The difference is 4 − 3 = 1. The equal-perimeter fact tempts a side-length computation (square side = 36/4 = 9 cm) that is irrelevant. 0, 2 and −1 mis-subtract.

**59. (B)** Congruent triangles have equal area, so compute ABC's area: drop the altitude from A to BC; half-base = 12, height = √(13² − 12²) = √(169 − 144) = √25 = 5, area = (1/2)(24)(5) = 60 cm². Hence DEF also has area 60 cm². 156 = (1/2)(24)(13) wrongly uses a side as the height; 120 doubles; 78 = (1/2)(12)(13) mixes pieces.

**60. (C)** (i) True: a single mirror line cannot be rotated onto another by any nontrivial rotation without creating a second mirror line, so the rotational order is 1 (e.g. an isosceles non-equilateral triangle). (ii) True: mirror lines, if any, come in a set whose count equals the rotational order; order 5 means either 0 mirror lines (pinwheel) or exactly 5 — both odd. (iii) False: two PERPENDICULAR mirror lines compose to a 180° rotation, giving order 2, not order 4 (order 4 would need mirror lines at 45°). So the pattern is T, T, F.
