# lh005_withanimation_allowlist.txt
#
# Grandfathered sites for LH005b — imperative `withAnimation(<X>) { ... }`
# wraps without a Reduce-Motion gate.
#
# Format:  <repo-relative path>:<line number>:<one-line reason>
#
# Empty lines and lines starting with `#` are ignored. Only the
# `path:line` prefix is the lookup key.
#
# This file was bootstrapped 2026-05-24 when LH005b landed. As of
# 2026-05-26 the allowlist is EMPTY — every grandfathered site has
# been migrated to `withAnimationRespectingReduceMotion(<X>) { ... }`
# in two sweeps:
#   - Round 1 (2026-05-26, b7de18f) — 54 sites across 8 Scene9 boss-quiz files
#   - Round 2 (2026-05-26, this commit) — 54 sites across 21 files
#     (DiscoverChapter* dispatchers, inline Discover scenes, Scene7
#     PitcherPlantTrap, top-level Favorites/History/Practice, plus
#     ChapterDetailView+HomeExperiments and QuestionDetailView)
#
# = 108 call sites migrated. The lint stays clean because the
# migrated sites no longer trigger LH005b. New violations are NOT
# allowed to be added here without a per-line reason that survives
# a code review.
#
# Preferred fixes — in order:
#   1. Inline gate:  withAnimation(reduceMotion ? nil : .easeInOut)
#   2. Helper:       withAnimationRespectingReduceMotion(.easeInOut)
#   3. Outer block:  wrap the call site in `if !reduceMotion { ... }`
#   4. Per-line escape:  `// lh005-ok: <reason>` on the same line
#      (last resort, used when the gate is provably elsewhere)

# (no grandfathered sites — the codebase is fully migrated)
